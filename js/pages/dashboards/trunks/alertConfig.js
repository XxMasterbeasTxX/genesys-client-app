/**
 * Trunk Alert — frontend defaults.
 *
 * Channel definitions are now served by the backend from
 * api/shared/channelConfig.js (single source of truth).
 * This file only holds UI-side fallback defaults.
 */

// ── Defaults (used when no config is saved yet) ───────────
export const DEFAULT_THRESHOLD = 0; // 0 = disabled
export const DEFAULT_COOLDOWN_MINUTES = 15;
